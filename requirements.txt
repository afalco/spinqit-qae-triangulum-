# Minimal runtime requirements for this repository.
# Pin versions only where hardware/backends are sensitive; otherwise keep flexible.

numpy>=1.23

# SpinQit provides Circuit/backends (simulator + NMR). Install via pip or your preferred method.
spinqit>=0.2.2

